import React from "react";

// notes: use react useForm from react-hook-form
// call your own validation schema
// for submit use the useMutation function from react-query

// example is on form example component

const TodoForm: React.FC = () => {
  return <div>{/* code here */}</div>;
};

export default TodoForm;
